machine:
  python:
    version: 3.5.2
  services:
    - docker
dependencies:
  override:
    - docker info
test:
  pre:
    - pip install alembic
    - pip install psycopg2
    - pip install airflow
    - pip install sqlalchemy
    - pip install pandas
  override:
    - cd ./tests && ./test.sh