machine:
  python:
    version: 3.6.0
  services:
    - docker
dependencies:
  override:
    - docker info
test:
  pre:
    - pip install psycopg2
    - pip install airflow
    - pip install sqlalchemy
    - pip install pandas
    - pip install defusedxml
  override:
    - cd ./tests && ./test.sh
